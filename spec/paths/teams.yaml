# /v1/teams
get:
  operationId: getTeams
  summary: Retrieves Teams
  description: >
    Returns Teams
  parameters:
    - $ref: ../components/parameters/parameters.yaml#/SportId
            #"season",
            #"activeStatus",
            #"leagueIds",
            #"sportId",
            #"sportIds",
            #"gameType",
            #"hydrate",
            #"fields",
  tags:
    - mlb
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: ../components/schemas/schemas.yaml#/Teams