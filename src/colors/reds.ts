import { createTheme } from "@mui/material"

const reds = createTheme({
  palette: {
    primary: {
      main: "#C6011F",
    },
    secondary: {
      main: "#000000",
    },
  },
})

export default reds
