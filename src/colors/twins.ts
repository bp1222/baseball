import { createTheme } from "@mui/material";

const twins = createTheme({
  palette: {
    primary: {
      main: "#002B5C",
    },
    secondary: {
      main: "#D31145",
    },
  },
});

export default twins;
