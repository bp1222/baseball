import { createTheme } from "@mui/material"

const rangers = createTheme({
  palette: {
    primary: {
      main: "#003278",
    },
    secondary: {
      main: "#C0111F",
    },
  },
})

export default rangers
