import { createTheme } from '@mui/material'

const astros = createTheme({
  palette: {
    primary: {
      main: '#002D62',
    },
    secondary: {
      main: '#EB6E1F',
    },
  },
})

export default astros
