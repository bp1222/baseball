import { createTheme } from "@mui/material"

const tigers = createTheme({
  palette: {
    primary: {
      main: "#0C2340",
    },
    secondary: {
      main: "#FA4616",
    },
  },
})

export default tigers
