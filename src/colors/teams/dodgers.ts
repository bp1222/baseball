import { createTheme } from "@mui/material"

const dodgers = createTheme({
  palette: {
    primary: {
      main: "#005A9C",
    },
    secondary: {
      main: "#FFFFFF",
    },
  },
})

export default dodgers
