import { createTheme } from '@mui/material'

const bluejays = createTheme({
  palette: {
    primary: {
      main: '#134A8E',
    },
    secondary: {
      main: '#E8291C',
    },
  },
})

export default bluejays
