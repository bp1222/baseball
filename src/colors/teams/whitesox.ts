import { createTheme } from '@mui/material'

const whitesox = createTheme({
  palette: {
    primary: {
      main: '#27251F',
    },
    secondary: {
      main: '#C4CED4',
    },
  },
})

export default whitesox
