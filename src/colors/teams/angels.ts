import { createTheme } from "@mui/material"

const angels = createTheme({
  palette: {
    primary: {
      main: "#BA0021",
    },
    secondary: {
      main: "#003263",
    },
  },
})

export default angels
