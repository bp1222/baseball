import { createTheme } from "@mui/material";

const braves = createTheme({
  palette: {
    primary: {
      main: "#13274F",
    },
    secondary: {
      main: "#CE1141",
    },
  },
});

export default braves;
