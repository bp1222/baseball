import { createTheme } from "@mui/material";

const royals = createTheme({
  palette: {
    primary: {
      main: "#004687",
    },
    secondary: {
      main: "#BD9B60",
    },
  },
});

export default royals;
