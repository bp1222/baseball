import { createTheme } from "@mui/material";

const mets = createTheme({
  palette: {
    primary: {
      main: "#002D72",
    },
    secondary: {
      main: "#FF5910",
    },
  },
});

export default mets;
