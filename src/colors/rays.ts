import { createTheme } from "@mui/material";

const rays = createTheme({
  palette: {
    primary: {
      main: "#092C5C",
    },
    secondary: {
      main: "#8FBCE6",
    },
  },
});

export default rays;
