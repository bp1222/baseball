import { createTheme } from "@mui/material";

const guardians = createTheme({
  palette: {
    primary: {
      main: "#E31937",
    },
    secondary: {
      main: "#0C2340",
    },
  },
});

export default guardians;
