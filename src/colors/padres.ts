import { createTheme } from "@mui/material"

const padres = createTheme({
  palette: {
    primary: {
      main: "#2F241D",
    },
    secondary: {
      main: "#FEC325",
    },
  },
})

export default padres
