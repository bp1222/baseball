import { createTheme } from "@mui/material";

const pirates = createTheme({
  palette: {
    primary: {
      main: "#FDB827",
    },
    secondary: {
      main: "#27251F",
    },
  },
});

export default pirates;
