/**
 * AppModals - Single location for rendering all application modals.
 *
 * Implementation lives in components/modals/; this file re-exports
 * so existing imports (e.g. from routes) continue to work.
 */

export { AppModals } from './modals'
