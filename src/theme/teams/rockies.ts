import { createTheme } from '@mui/material'

const rockies = createTheme({
  palette: {
    primary: {
      main: '#33006F',
    },
    secondary: {
      main: '#C4CED4',
    },
  },
})

export default rockies
