import { createTheme } from '@mui/material'

const redsox = createTheme({
  palette: {
    primary: {
      main: '#BD3039',
    },
    secondary: {
      main: '#0D2B56',
    },
  },
})

export default redsox
