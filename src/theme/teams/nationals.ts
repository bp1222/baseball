import { createTheme } from '@mui/material'

const nationals = createTheme({
  palette: {
    primary: {
      main: '#AB0003',
    },
    secondary: {
      main: '#14225A',
    },
  },
})

export default nationals
