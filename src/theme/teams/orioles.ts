import { createTheme } from '@mui/material'

const orioles = createTheme({
  palette: {
    primary: {
      main: '#DF4601',
    },
    secondary: {
      main: '#000000',
    },
  },
})

export default orioles
